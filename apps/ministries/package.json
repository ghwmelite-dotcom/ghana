{
  "name": "@gh/ministries",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "echo '@gh/ministries: no typecheck yet' && exit 0",
    "lint": "echo '@gh/ministries: no lint yet' && exit 0",
    "test": "echo '@gh/ministries: no test yet' && exit 0",
    "build": "echo '@gh/ministries: no build yet' && exit 0"
  }
}
