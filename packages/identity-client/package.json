{
  "name": "@gh/identity-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "echo '@gh/identity-client: no typecheck yet' && exit 0",
    "lint": "echo '@gh/identity-client: no lint yet' && exit 0",
    "test": "echo '@gh/identity-client: no test yet' && exit 0",
    "build": "echo '@gh/identity-client: no build yet' && exit 0"
  }
}
