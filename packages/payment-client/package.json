{
  "name": "@gh/payment-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "echo '@gh/payment-client: no typecheck yet' && exit 0",
    "lint": "echo '@gh/payment-client: no lint yet' && exit 0",
    "test": "echo '@gh/payment-client: no test yet' && exit 0",
    "build": "echo '@gh/payment-client: no build yet' && exit 0"
  }
}
