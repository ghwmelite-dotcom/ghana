{
  "name": "@gh/schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "echo '@gh/schemas: no typecheck yet' && exit 0",
    "lint": "echo '@gh/schemas: no lint yet' && exit 0",
    "test": "echo '@gh/schemas: no test yet' && exit 0",
    "build": "echo '@gh/schemas: no build yet' && exit 0"
  }
}
