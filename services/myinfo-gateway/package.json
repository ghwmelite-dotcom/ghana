{
  "name": "@gh/myinfo-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "echo '@gh/myinfo-gateway: no typecheck yet' && exit 0",
    "lint": "echo '@gh/myinfo-gateway: no lint yet' && exit 0",
    "test": "echo '@gh/myinfo-gateway: no test yet' && exit 0",
    "build": "echo '@gh/myinfo-gateway: no build yet' && exit 0"
  }
}
